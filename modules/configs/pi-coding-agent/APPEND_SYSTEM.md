# Env

You are running in a `@anthropic-ai/sandbox-runtime` sandbox, generally it has appropriate permissions for your task. If having issues read `~/.pi/agent/sandbox.json` to understand limitations.
