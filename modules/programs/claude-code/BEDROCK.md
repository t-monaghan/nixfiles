# Bedrock configuration

Claude Code uses AWS Bedrock inference profiles for model access. Configuration is managed through an external bedrock.nix file.

## Setup

Profile configuration is loaded from `~/.config/nixfiles/bedrock.nix` at build time.

See internal documentation for setup instructions.
